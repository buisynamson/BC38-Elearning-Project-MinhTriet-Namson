const sliceName = "home";

const actions = {
  SET_COURSE_CATEGORY: `${sliceName}/SET_COURSE_CATEGORY`,
  SET_COURSE_LIST_PAGE: `${sliceName}/SET_COURSE_LIST_PAGE`,
  SET_DETAIL_COURSE: `${sliceName}/SET_DETAIL_COURSE`
};

export default actions;
