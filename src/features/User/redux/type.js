const sliceName = "user";

const actions = {
  USER_SIGNIN: `${sliceName}/USER_SIGNIN`,
  USER_SIGNUP: `${sliceName}/USER_SIGNUP`,
  USER_LOGGED: `${sliceName}/USER_LOGGED`
};

export default actions;
